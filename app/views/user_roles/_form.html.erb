<%= form_for(@user_role, remote: true) do |f| %>
  <% if @user_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_role.errors.count, "error") %> prohibited this user_role from being saved:</h2>

      <ul>
      <% @user_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.collection_select(:user_id,@user,:id, :id_name) %></p>

    <% for i in 0..@role.count-1 %>

        <input id="role_grant_grant_id" type="checkbox" value="<%= @role[i].id %>" name="role_id[]">
        <label><%=  @role[i].role_name%></label>
        <br/>
    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
